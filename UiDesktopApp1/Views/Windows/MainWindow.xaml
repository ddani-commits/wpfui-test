<ui:FluentWindow
    x:Class="UiDesktopApp1.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:UiDesktopApp1.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{Binding ViewModel.ApplicationTitle, Mode=OneWay}"
    Width="1100"
    Height="650"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    WindowBackdropType="None"
    mc:Ignorable="d">
    <!--
    -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <ui:TitleBar
            x:Name="TitleBar"
            Grid.Row="0"
            Grid.Column="1"
            CloseWindowByDoubleClickOnIcon="False">
            </ui:TitleBar>

            <ui:NavigationView
            x:Name="RootNavigation"
            Grid.Row="0"
            Grid.Column="0"
            Padding="42,0,42,0"
            BreadcrumbBar="{Binding ElementName=BreadcrumbBar}"
            FooterMenuItemsSource="{Binding ViewModel.FooterMenuItems, Mode=OneWay}"
            FrameMargin="0"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleVisible="True"
            MenuItemsSource="{Binding ViewModel.MenuItems, Mode=OneWay}"
            PaneDisplayMode="LeftFluent"
            
            MouseLeftButtonDown="RootNavigation_MouseLeftButtonDown"
            >
                <ui:NavigationView.Header>
                    <ui:BreadcrumbBar x:Name="BreadcrumbBar" Margin="42,32,42,20" />
                </ui:NavigationView.Header>
                <ui:NavigationView.ContentOverlay>
                    <Grid>
                        <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
                    </Grid>
                </ui:NavigationView.ContentOverlay>
                <ui:NavigationView.FooterMenuItems>
                    <ui:NavigationViewItem Width="40" Height="40"  Content="" Click="MinimizeWindowButton" ></ui:NavigationViewItem>
                    <ui:NavigationViewItem Width="40" Height="40"  Content="" Click="Maximize_Click" ></ui:NavigationViewItem>
                    <ui:NavigationViewItem Width="40" Height="40" Content="" Click="NavigationViewItem_Click"></ui:NavigationViewItem>
                </ui:NavigationView.FooterMenuItems>
            </ui:NavigationView>

        <ContentPresenter
            x:Name="RootContentDialog"
            Grid.Row="1"
            Grid.RowSpan="2" />
    </Grid>
</ui:FluentWindow>
